server:
  port: 60000

spring:
  application:
    name: user-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: true
#      path: /h2-console
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
#    username: sa
#    password:
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: $2a$10$Weo1HBUQcCPfJJ.ijKlX1Oj9m6zbxzMr3rxODBXI0l9bH.Kdforqe

eureka:
  instance:
    # 호스트네임 대신 IP 주소를 사용하여 유레카에 등록 (DNS 에러 해결)
    prefer-ip-address: true
    # 인스턴스 ID도 IP를 포함하도록 구성 (관리하기 더 편해집니다)
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    fetch-registry: true
    register-with-eureka: true

greeting:
  message: Welcome to the Simple E-commerce.

#token:
#  expiration-time: 86400000 #10 days
#  secret: my-very-long-secret-key-1234567890123456

logging:
  level:
    org.springframework.security: DEBUG
    com.example.userservice.client: DEBUG

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          - beans
          - info
          - busrefresh